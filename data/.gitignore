# Ignore everything in data/ by default
*

# Keep this ignore file
!.gitignore

# Keep governance directory and DVC tracking file
!/governance/
!/governance/dataset_checksums.json.dvc

# Keep raw-data documentation (but NOT the actual dataset blobs)
!/raw/
!/raw/README.md
!/raw/.gitkeep
